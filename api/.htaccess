# .htaccess for /api
# Deny all access to the api directory. Use this when you want no external access.

# Disable directory listing
Options -Indexes

# Apache 2.4+
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>

# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>

# End of file